import re
user_input=input("ввод: ")
numbers_int=re.findall(r'-?\d+', user_input)
input_array = [int(x) for x in numbers_int]
if not input_array:
    print("массив пуст")
else:
    max_num = input_array[0]
    for num in input_array:
        if num>max_num:
            max_num=num
print(max_num)
